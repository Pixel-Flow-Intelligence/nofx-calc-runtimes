[project]
name = "pandas-ta-runtime"
version = "0.1.0"
description = "Pandas TA Python bridge dependencies"
requires-python = ">=3.12"
dependencies = [
  "pandas>=2.2,<3.0",
  "pandas-ta-classic",
]

[tool.uv]
package = false
